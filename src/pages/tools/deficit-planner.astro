---
import ToolLayout from '../../layouts/ToolLayout.astro';
import DeficitPlanner from '../../components/DeficitPlanner.jsx';
---
<ToolLayout slug="deficit-planner" title="Deficit & Goal-Date Planner" intro="A dynamic model that accounts for metabolic slowing, so the projection flattens the way real weight loss does.">
  <DeficitPlanner client:load  />
</ToolLayout>
