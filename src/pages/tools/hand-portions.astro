---
import ToolLayout from '../../layouts/ToolLayout.astro';
import HandPortions from '../../components/HandPortions.jsx';
---
<ToolLayout slug="hand-portions" title="Hand-Portion Translator" intro="Converts macro targets into palms, cupped hands and thumbs, so tracking works without a scale.">
  <HandPortions client:load />
</ToolLayout>
