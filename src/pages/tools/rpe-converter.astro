---
import ToolLayout from '../../layouts/ToolLayout.astro';
import RpeConverter from '../../components/RpeConverter.jsx';
---
<ToolLayout slug="rpe-converter" title="RPE, RIR & %1RM Converter" intro="Prescribe working loads by reps in reserve rather than a fixed percentage, so the weight adjusts to how you are actually performing.">
  <RpeConverter client:load />
</ToolLayout>
