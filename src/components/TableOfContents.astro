---
/** Jump links. Pass items as [{ id, label }]. */
const { items = [] } = Astro.props;
---
{items.length > 0 && (
  <nav class="toc" aria-label="Article contents">
    {items.map(i => <a href={`#${i.id}`}>{i.label}</a>)}
  </nav>
)}
